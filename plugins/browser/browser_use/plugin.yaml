name: browser-browser-use
version: 1.0.0
description: "Browser Use (https://browser-use.com) cloud browser backend. Supports both direct BROWSER_USE_API_KEY and the managed JARVIS Managed tool gateway. Also powers the 'JARVIS Managed' UX flow that bills usage to a JARVIS Managed subscription."
author: JARVISProject
kind: backend
provides_browser_providers:
  - browser-use
